import session from './session';
import user from './user';
import message from './message';
import sessionStub from './session-stub';
import userStub from './user-stub';
import messageStub from './message-stub';

// Export the routes in this module for the application's index.js to use
export default {
  session,
  user,
  message,
  sessionStub,
  userStub,
  messageStub,
};
